(jbuild_version 1)

(executable
 ((name test)
  (modules (test))
  (libraries (omd))))

(alias
 ((name runtest)
  (deps (test.exe))
  (action (progn (chdir ../../../../tests/cow (run ${<}))))))
